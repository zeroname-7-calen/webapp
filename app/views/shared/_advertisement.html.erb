<h6><%= link_to "カルーセルテスト画面", top_test_path %></h6>

<!-- 広告一覧ボタン -->
<%# <% if current_user %>
  <div class="ad_tool">
    <h6><%= link_to "広告一覧確認", advertisements_path %></h6>
  </div>
<%# <% end %>

<div class="advertisement">
  <% @advertisements = Advertisement.order(created_at: :desc) %>
  <% if @advertisements.present? %>
    <% @advertisements.each do |advertisement| %>
      <li><%= image_tag advertisement.picture.url if advertisement.picture? %></li>
    <% end %>
  <% else %>
    <p>広告掲載はありません</p>
  <% end %>
</div>
